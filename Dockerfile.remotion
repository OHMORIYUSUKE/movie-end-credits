FROM node:22

WORKDIR /app

COPY package.json yarn.lock* ./

RUN yarn install --frozen-lockfile

COPY . .

# We'll use 3001 for Remotion Studio to avoid conflict with Next.js (3000)
EXPOSE 3001

CMD ["yarn", "remotion", "studio", "--port=3001", "--host=0.0.0.0"]
